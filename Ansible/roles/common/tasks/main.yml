---
- name: Enable UFW
  ufw:
    state: enabled

- name: Enable UFW logging
  ufw:
    logging: 'True'

- name: Allow SSH access from internal networks
  ufw:
    rule: allow
    port: '22'
    proto: tcp
    src: '{{ item }}'
  with_items:
    - <REDACTED>/24
    - <REDACTED>/24
